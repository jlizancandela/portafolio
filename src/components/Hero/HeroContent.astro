---
import HeroButtons from "./HeroButtons.astro";
---

<div class="hero__content">
  <h1>
    Hola 👋 Soy <span>Jorge Lizan</span>, desarrollador web Trainee en proceso
    de aprendizaje.
  </h1>
  <HeroButtons />
</div>

<style>
  @media (max-width: 800px) {
    .hero__content {
      align-items: center;
      text-align: center;
    }
  }
  .hero__content {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    justify-content: center;
    gap: 2rem;
  }

  .hero__content > h1 {
    /* Tipografía fluida para evitar cortes en pantallas pequeñas */
    font-size: clamp(1.5rem, 5.5vw, 2.5rem);
    line-height: 1.2;
    font-weight: bold;
    margin: 0;
    text-wrap: balance;
  }

  .hero__content > h1 > span {
    background: linear-gradient(purple, blue);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    /* Mantener el mismo tamaño que el h1 para no desbordar */
    font-size: inherit;
  }
</style>
